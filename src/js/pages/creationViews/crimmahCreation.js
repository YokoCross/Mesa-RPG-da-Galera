import React, { Component } from 'react'
import { connect } from 'react-redux'

export class CrimmahCreation extends Component {
    render() {
        return (
            <div>
                CrimmahCreation
            </div>
        )
    }
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(CrimmahCreation)
